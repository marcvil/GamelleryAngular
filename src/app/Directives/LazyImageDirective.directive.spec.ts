/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LazyImageDirectiveDirective } from './LazyImageDirective.directive';

describe('Directive: LazyImageDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImageDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
